exports.uploadFile = async (req, res, next) => {
  //console.log(`Uploading image ${req.body.uploaded_file} to folder ${req.params.folderId} owned by user ${req.user.id}`)
  //console.log(req.file)
  res.redirect("/");
}